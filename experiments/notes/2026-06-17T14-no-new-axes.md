# L5 Research: No new axes — floor confirmed, three new commits audited

**Date:** 2026-06-17T14 UTC  
**Branch:** kannaka-curiosity/2026-06-17T14-no-new-axes  
**Code changes:** NONE  
**Status:** No new hypotheses. No trials run.

---

## Orientation

Current empirical optimum: `DRIVE_A=0.15 DRIVE_SCOPE=all DREAM_MODE=interference_relax` → fitness **0.0578** (confirmed deterministic, ≥10 trials across T09–T11).

Three new commits since the last fire (T12, 2026-06-16T12):

| commit  | change | L5 impact |
|---------|--------|-----------|
| `25f2eef` | fix(working_memory): single wavefront returns attention_coherence 1.0 not 0.0 | None — `attention_coherence` is not read by consolidation.rs or any L5 metric path |
| `a0fa99c` | refactor(invariant): drop dead `_best_reconstruction` variable in compute_delta | None — dead variable removal |
| `4901dd2` | fix(research): remove debug println! from eval_xi_diversity | None — removes stdout noise; computed values and TSV rows unchanged |

None of these opens a new parameter or code-path axis.

---

## All six research questions remain closed (per T09/T12)

1. **3-run interference_relax characterization** — done (≥10 trials, avg 0.0578).
2. **K-sweep under fixed plumbing** — irx bypasses Kuramoto; K irrelevant.
3. **irx + xi recovery via relax_steps** — already at 16/20; relax_steps=24 makes carrier_e worse.
4. **R-xi correlation at stage_sync** — irx bypasses stage_sync; study moot.
5. **Φ ↔ R relationship** — characterization only; no fitness path.
6. **Drive frequency variants** — carrier_e structurally insensitive under ceiling saturation.

---

## Decision

**No code changes. No trials run. No improvement found.**

The structural floor at fitness ≈ 0.058 is mathematically proven (T09 DFT analysis: carrier_e = 0.533 requires changing the amplitude-delta pattern from an impulse to a ramp, which is incompatible with the xi evaluation constraint at chain_depth=2). All parameter-sweep and small-code-change axes are exhausted.
