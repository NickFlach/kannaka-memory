# L5 Research: No new axes — optimization surface remains bounded

**Date:** 2026-06-16T12 UTC
**Branch:** kannaka-curiosity/2026-06-16T12-no-new-axes
**Code changes:** NONE
**Status:** No new hypotheses. No trials run.

---

## Orientation findings

Current empirical optimum: `DRIVE_A=0.15 DRIVE_SCOPE=all DREAM_MODE=interference_relax` → fitness **0.0578** (confirmed deterministic, T11 baseline).

191 commits merged since previous master snapshot, including:
- `fix(consolidation): clamp strengthen amplitude + reclaim ghosts (#360, #361)` — amplitude overflow in particle-consolidation path clamped to AMPLITUDE_CEILING. Already reflected in T09-T11 baseline fitness (0.0578 confirmed post-fix).
- `fix(consolidation): clamp consolidate_modality_aware amplitude (#368)` — same, no new L5 axis.
- New files: `src/research_planner.rs`, `src/sensemaking.rs`, `src/swarm_fitness.rs`, `src/swarm_loop.rs`, `src/temporal.rs` — L6 infrastructure (collective OODA arm), no L5 metric or parameter changes.

All six briefing research questions remain closed (per T09 comprehensive review):
1. 3-run irx characterization — done (≥10 trials, avg 0.0578).
2. K-sweep under fixed plumbing — closed (irx bypasses Kuramoto).
3. irx + xi recovery via relax_steps — closed (already at 16/20; 24 makes carrier_e worse).
4. R-xi correlation at stage_sync — closed (irx bypasses stage_sync; study is moot).
5. Φ ↔ R characterization — characterization only (magic_proxy_phase_R not in fitness formula; no fitness path found).
6. Drive frequency variants — closed (T13: carrier_e insensitive to DRIVE_FREQ_HZ under ceiling saturation).

## This fire's additional analysis

### Drive timing verification

`t = cycle_idx × 0.125 s`, `DRIVE_FREQ_HZ = 0.5 Hz`. Drive factors across chain_depth=4:
- cycle 0: sin(0) = 0 → factor 1.00 (no drive)
- cycle 1: sin(π/8) ≈ 0.383 → factor 1.057
- cycle 2: sin(π/4) ≈ 0.707 → factor 1.106
- cycle 3: sin(3π/8) ≈ 0.924 → factor 1.139

Drive is monotonically increasing (quarter-period arc) and never negative in cycles 0-3. Combined with constructive pair recovery (49 pairs × 0.45 = 22.05 per cycle), ceiling-saturated memories cannot produce non-zero delta in cycles 1-3 regardless of drive frequency, drive amplitude (within A<0.3), or phase offset.

### Alternative drive frequency analysis

Frequencies that could create alternating drive pattern across 4 cycles (positive at k=1,3; negative at k=2):
- Requires 2.67 Hz < freq < 4.0 Hz
- At A=0.8 (forbidden: A≥0.3) with zero constructive pairs: carrier_e ≈ 0.889 theoretically
- But zero constructive pairs collapses xi and transfer; A≥0.3 is known-bad
- No valid parameter combination in allowed region achieves this

### carrier_emergence computation clarification

Carrier emergence is measured on `engine_flat` (flat-frequency corpus, all 300 memories at 0.1 Hz), NOT on the bimodal corpus_a. Same ceiling-saturation physics apply: amp_deltas_flat = [~0.95, ~0.031, ~0.003, ~0.036]. DFT gives k=1 power ≈ k=2 power → carrier_e ≈ 0.533. Floor is structural, not corpus-dependent.

## Decision

**No code changes. No trials run. No improvement found.**

The optimization surface is confirmed bounded at fitness ≈ 0.058. All single-fire parameter and small-code-change axes are exhausted. Improvement requires architectural change (relative amplitude ceiling, pair density reduction, or drive-correlated pair formation), confirmed out of single-fire scope across T09-T11.
