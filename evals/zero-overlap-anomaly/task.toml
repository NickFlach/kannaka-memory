schema_version = "1.3"
artifacts = []

[task]
name = "kannaka/zero-overlap-anomaly"
description = "Semantic anomaly probe: 33 queries sharing ZERO >=4-char tokens with their target's full content, against the frozen HRM snapshot. Any top-10 hit is retrieval a lexical encoder cannot explain. Reward = recall@10 on the zero-overlap set."
keywords = ["retrieval", "semantic", "anomaly", "kannaka", "recall"]
[[task.authors]]
name = "Nick Flach"

[metadata]
snapshot_sha256 = "339e7ad90bda2d035d05ab72342e6c73a6bdf5d9f2079f1241d759fcdf466d7a"
binary_release = "v0.13.0 kannaka-linux-x86_64"
probes = "8 v2 probes already at zero overlap + 25 near-zero v2 probes rewritten to strict zero; invariant re-verified by the verifier"
reference = "baseline-002 subset: 2/8 original zero-overlap probes hit (p31@3, p43@6)"

[verifier]
timeout_sec = 900.0
collect = []

[verifier.env]

[agent]
timeout_sec = 3600.0

[environment]
network_mode = "no-network"
build_timeout_sec = 1800.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
