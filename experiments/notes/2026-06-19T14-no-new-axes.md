# L5 Research: No new axes — six post-T18 commits audited, floor confirmed

**Date:** 2026-06-19T14 UTC
**Branch:** kannaka-curiosity/2026-06-19T14-no-new-axes
**Code changes:** NONE
**Status:** No new hypotheses. No trials run.

---

## Orientation

Current empirical optimum: `DRIVE_A=0.15 DRIVE_SCOPE=all DREAM_MODE=interference_relax` → fitness **0.0578** (confirmed deterministic, ≥10 trials across T09–T12, floor mathematically proven in T09 DFT analysis).

Six new commits since T18 (2026-06-18T14):

| commit  | change | L5 impact |
|---------|--------|-----------|
| `2fea880` | feat(consolidation): ADR-0036 Phase 1 — flow reactivation from recall (merge-on-write) | None — adds `flush_reactivation` and `save_reactivation_merge` to `HrmStore`; `MediumBackend::flush_reactivation` default is a no-op; research uses `TestMedium` which inherits the no-op default |
| `cf29757` | fix(consolidation): ADR-0036 Phase 1 — record reactivation on the production recall path | None — adds `record_retrieval()` bumps in `KannakaMemorySystem::recall`; research.rs uses `ResonanceEngine` directly, never calls `KannakaMemorySystem::recall` |
| `4950c68` | fix(persistence): sweep orphaned HRM tmp files after chiral save (disk-fill fix) | None — disk maintenance inside `ChiralMedium::save_snapshot`; only runs in production HRM saves, not in research path |
| `2935c15` | feat(dream): single-writer lock — stop the lite-dream double-writer | None — changes only `src/bin/kannaka.rs` (the CLI binary); research binary is unaffected |
| `61ab1bf` | feat(consolidation): ADR-0036 Phase 1 — replay-gated promotion (reactivation → LongTerm) | None — adds a second promotion gate to `KannakaMemorySystem::promote_strengthened_short_term`; this function is in `openclaw.rs` and is never called from the research binary |
| `dab3c92` | release: v0.6.32 | None — version bump only |

### Key structural confirmation

`git diff 151137c dab3c92 -- src/consolidation.rs` → 0 lines changed.
`git diff 151137c dab3c92 -- src/bin/research.rs` → 0 lines changed.

Neither the dream consolidation logic nor the L5 research binary changed. All six commits touch only the production path (KannakaMemorySystem, HrmStore, ChiralMedium, bin/kannaka.rs).

---

## All six research questions remain closed (per T09/T12/T17/T18)

1. **3-run interference_relax characterization** — done (≥10 trials, avg 0.0578).
2. **K-sweep under fixed plumbing** — irx bypasses Kuramoto; K irrelevant.
3. **irx + xi recovery via relax_steps** — already at 16/20; relax_steps=24 makes carrier_e worse.
4. **R-xi correlation at stage_sync** — irx bypasses stage_sync; study moot.
5. **Φ ↔ R relationship** — characterization only; no fitness path.
6. **Drive frequency variants** — carrier_e structurally insensitive under ceiling saturation.

---

## Decision

**No code changes. No trials run. No improvement found.**

The structural floor at fitness ≈ 0.058 holds. ADR-0036 Phase 2 (apply mode, actual memory mutation during consolidation) is the next architectural change that may open new optimization axes when it lands.
