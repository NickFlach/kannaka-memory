//! Fail-closed channel policy for the Hive bridge (ADR-0045). Task 6 fills
//! this in with `PolicyMap` — the type that decides which channels forward
//! and which agent kinds are allowed per channel. Placeholder so the
//! `hive_bridge` module tree compiles ahead of that work.
