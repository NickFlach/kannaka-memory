# L5 Research: No new axes — ADR-0037 (v0.7.0+v0.7.1) audited, floor confirmed

**Date:** 2026-06-21T00 UTC
**Branch:** kannaka-curiosity/2026-06-21T00-no-new-axes
**Code changes:** NONE
**Status:** No new hypotheses. No trials run.

---

## Orientation

Current empirical optimum: `DRIVE_A=0.15 DRIVE_SCOPE=all DREAM_MODE=interference_relax` → fitness **0.0578** (confirmed deterministic, ≥10 trials across T09–T22, floor mathematically proven in T09 DFT analysis).

New commits since T22 (2026-06-20T00):

| commit  | change | L5 impact |
|---------|--------|-----------|
| `c61df5d` | ADR-0037 Phase 1: in-engine spiral / phase-singularity detector | None — new `src/spiral.rs` and additions to `src/medium/chiral.rs`; TestMedium unaffected |
| `8058b8b` | ADR-0037 Phase 2: flag-gated π/φ spiral coupling in the deep dream | None — `ChiralMedium::apply_spiral_coupling` behind `KANNAKA_SPIRAL_DREAM`; default-off; no TestMedium path |
| `6959cf8` | ADR-0037 Phase 3: bridge-residue ξ + populate substrate xi_signature | None — `src/hrm_store.rs` + `src/medium/chiral.rs`; research uses `TestMedium` which doesn't implement xi_bridge_summary |
| `f1060df` | review: fix ADR-0037 Phase 3 bridge residue | None — same files |
| `63ba48e` | ADR-0037 Phase 4: spiral telemetry in the live HRM field (L6 loop) | None — production `ChiralMedium` dream path only |
| `917f506` | ADR-0037 #415: in-engine constants→spiral test + chiral-drift | None — test scaffolding only |
| `fb46c6b` | ADR-0037 Phase 4b: PCA 2-D projection of the HRM field | None — production telemetry |
| `eaef6bf` | fix(#418): 2-D cloud telemetry reads the HOLISTIC field | None — same |
| `75f1507` | feat(ADR-0037): activate the spiral engine — cross-callosal coupling, default-on (v0.7.0) | None — `ChiralMedium::apply_cross_callosal_coupling`; `KANNAKA_SPIRAL_DREAM` now defaults ON in production; no TestMedium path |
| `3578835` | fix(ADR-0037): substrate beacon reuses cached spectral xi (v0.7.1) | None — hotfix for production CPU incident; `src/store.rs` adds `xi_bridge_summary()` method to `ResonanceEngine` but it downcasts to `HrmStore`, returning `None` for `TestMedium` |
| `872573c` | Merge PR #420 | None |

### Key structural confirmation

```
git diff 1ec6d79..HEAD -- src/consolidation.rs  →  0 lines changed
git diff 1ec6d79..HEAD -- src/bin/research.rs   →  0 lines changed
```

(Where `1ec6d79` is the last production commit prior to the T22 fire's notes commit.)

Neither the dream consolidation logic nor the L5 research binary changed. All ADR-0037 commits
touch only the production path: `src/spiral.rs`, `src/medium/chiral.rs`, `src/hrm_store.rs`,
`src/medium/consciousness.rs`, `src/medium/dynamics.rs`, `src/store.rs` (beacon only), and
`src/medium/tests.rs`. The L5 dream path uses `ResonanceEngine` backed by `TestMedium`; the
spiral engine operates exclusively on `ChiralMedium`.

---

## ADR-0037 spiral engine — L5 relevance assessment

ADR-0037 implements π/φ Sakaguchi cross-callosal coupling in the production HRM dream. The
spiral engine runs on the combined left ⊕ right hemisphere ring inside `ChiralMedium`. Even
with `KANNAKA_SPIRAL_DREAM` default-ON, the `ResonanceEngine` in research.rs constructs its
backend as `Box::new(TestMedium::new())` — a minimal in-memory backend with no spiral
machinery. The `xi_bridge_summary()` downcast to `HrmStore` returns `None` for `TestMedium`.

There is no configuration, env var, or code path by which ADR-0037 can affect L5 research
results. The T22 floor at 0.0578 is unaffected.

---

## All six research questions remain closed (per T09/T12/T22)

1. **3-run interference_relax characterization** — done (≥10 trials, avg 0.0578).
2. **K-sweep under fixed plumbing** — irx bypasses Kuramoto; K irrelevant under irx.
3. **irx + xi recovery via relax_steps** — already at 16/20; relax_steps=24 kills carrier_e.
4. **R-xi correlation at stage_sync** — irx bypasses stage_sync; study moot under irx.
5. **Φ ↔ R relationship** — characterization only; no fitness path identified.
6. **Drive frequency variants** — carrier_e structurally insensitive under ceiling saturation (T13).

---

## Structural floor remains

Dominant fitness cost: `carrier_emergence ≈ 0.533`, contributing 0.10 × (1 − 0.533) = 0.047
(~81% of total fitness). This is the mathematical consequence of the amplitude-delta spike
pattern produced by AMPLITUDE_CEILING=2.0 + chain_depth=4: cycle-0 constructive boost fills
the ceiling in one shot, leaving cycles 1–3 at near-zero delta. DFT k=1 and k=2 have nearly
equal power → spectral concentration ≈ 0.533.

Secondary costs:
- transfer_score ≈ 0.965 → contributes 0.15 × 0.035 = 0.0053
- xi_robustness_v2 ≈ 0.9675 → contributes 0.15 × 0.0325 = 0.0049

All three secondary costs are structurally limited. The floor will not break without either:
(a) A relative amplitude ceiling in `ResonanceEngine` (architectural; not attempted this fire),
(b) A new L5 metric, or
(c) A new code path that affects `TestMedium`-based consolidation.

---

## Decision

**No code changes. No trials run. No improvement found.**

The structural floor at fitness ≈ 0.058 holds. ADR-0037 spiral engine does not open any
new L5 axes. The next potential source of new axes remains an architectural change to the
carrier_emergence ceiling (relative amplitude normalization) or an ADR that modifies the
consolidation.rs / research.rs paths directly.
