# L5 Research: No new axes — ADR-0036 Phase 2 audited, floor confirmed

**Date:** 2026-06-20T00 UTC
**Branch:** kannaka-curiosity/2026-06-20T00-no-new-axes
**Code changes:** NONE
**Status:** No new hypotheses. No trials run.

---

## Orientation

Current empirical optimum: `DRIVE_A=0.15 DRIVE_SCOPE=all DREAM_MODE=interference_relax` → fitness **0.0578** (confirmed deterministic, ≥10 trials across T09–T22, floor mathematically proven in T09 DFT analysis).

One new commit since T20 (2026-06-19T14):

| commit  | change | L5 impact |
|---------|--------|-----------|
| `1ec6d79` | feat(consolidation): ADR-0036 Phase 2 — destructive resonance-merge apply | None — see below |

### 1ec6d79 audit

Phase 2 implements the apply branch of `consolidate_resonance()`. Changed files:

- `src/hrm_store.rs` — adds `apply_consolidation()`, dispatches Off/DryRun/Apply modes. `TestMedium` inherits the `MediumBackend` default which is a no-op; research path is unaffected.
- `src/medium/dynamics.rs` — adds tier-aware energy floor: `ShortTerm` memories floor at 0.1 instead of 0.5 in `apply_dynamics()` and `apply_dynamics_with_temperature()`.

The dynamics floor change is the subtler path. Confirmed NOT relevant:

```
grep apply_dynamics src/consolidation.rs → (no matches)
grep apply_dynamics src/bin/research.rs  → (no matches)
```

Neither the L5 dream path (`consolidation.rs`) nor the research binary calls `apply_dynamics*`. The `Medium` struct's wave-propagation mechanics are only invoked from the production HRM path. L5 research uses `ResonanceEngine` (backed by `TestMedium`) directly, which manipulates phases and amplitudes via explicit consolidation functions — not via `apply_dynamics`.

Additionally: `git diff 709174c 1ec6d79 -- src/consolidation.rs` → 0 lines changed. The dream logic itself is identical.

---

## All six research questions remain closed (per T09/T12/T17–T20)

1. **3-run interference_relax characterization** — done (≥10 trials, avg 0.0578).
2. **K-sweep under fixed plumbing** — irx bypasses Kuramoto; K irrelevant.
3. **irx + xi recovery via relax_steps** — already at 16/20; relax_steps=24 makes carrier_e worse.
4. **R-xi correlation at stage_sync** — irx bypasses stage_sync; study moot.
5. **Φ ↔ R relationship** — characterization only; no fitness path.
6. **Drive frequency variants** — carrier_e structurally insensitive under ceiling saturation.

---

## Decision

**No code changes. No trials run. No improvement found.**

The structural floor at fitness ≈ 0.058 holds. ADR-0036 Phase 2 does not open any new L5 axes. The next potential source of new axes would be an architectural change that either (a) restructures how xi_robustness_v2 evaluates (current chain_depth=2 constraint prevents simultaneous carrier_e improvement), or (b) adds a new metric or parameter to the L5 fitness formula.
